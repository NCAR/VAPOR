#version 330 compatibility

// Input vertex data
layout(location = 0) in vec3 vertexPosition;

void main(void)
{
    // Caution:   gl_ModelViewProjectionMatrix is NOT available in core profile. 
    gl_Position = gl_ModelViewProjectionMatrix * vec4( vertexPosition, 1 );
}
