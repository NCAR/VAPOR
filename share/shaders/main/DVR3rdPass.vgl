#version 410 core

// Input vertex positions, model coordinate
layout(location = 0) in vec3 vertexPosition;

uniform mat4 MVP;

out vec4 positionClip;   // vertex position in the clip space

void main(void)
{
    positionClip    = MVP * vec4( vertexPosition, 1.0f );

    gl_Position     = positionClip;
}
